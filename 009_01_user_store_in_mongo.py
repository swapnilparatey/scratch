# Idea is to create a bunch of USERS as JSON objects - flatten them into a pickle
# And then create a key out of it using some shutil OR whatever -
# And then store it in MongoDB in an appropriate way
# And maybe even figure out if this is the right way to do it

